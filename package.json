{
	"name": "cspell-grammar-repo",
	"description": "A tmLanguage based Grammar Parser for cSpell",
	"devDependencies": {
		"coveralls": "^3.0.3",
		"lerna": "^3.14.1",
		"nyc": "^14.1.1",
		"rimraf": "^2.6.3"
	},
	"scripts": {
		"test": "npm run lerna-test",
		"build": "npm run lerna-build",
		"bootstrap": "lerna bootstrap",
		"lint-travis": "lerna run lint-travis",
		"lerna-install": "lerna bootstrap",
		"lerna-install-ci": "lerna bootstrap --ci",
		"lerna-build": "lerna run build",
		"lerna-test": "lerna run test",
		"coverage": "rimraf .nyc_output && npm run generate-code-coverage",
		"generate-code-coverage": "lerna run generate-code-coverage",
		"coverage-coveralls": "npm run coverage && nyc report --reporter=text-lcov | coveralls"
	},
	"author": "Jason Dent",
	"license": "MIT",
	"private": true,
	"bugs": {
		"url": "https://github.com/Jason3S/cspell-grammar/issues"
	},
	"homepage": "https://github.com/Jason3S/cspell-grammar#readme",
	"nyc": {
		"include": [
			"src/**/*.ts"
		],
		"exclude": [
			"src/**/*.test.ts"
		],
		"extension": [
			".ts"
		],
		"require": [
			"ts-node/register"
		],
		"clean": false,
		"temp-directory": ".nyc_output",
		"report-dir": "coverage",
		"reporter": [
			"json",
			"html"
		]
	}
}
